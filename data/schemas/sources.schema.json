{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Barista Sources Registry",
  "description": "Registry of data sources for the Barista application",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Display name of the source"
      },
      "description": {
        "type": "string",
        "description": "Short description of the content provided by this source"
      },
      "updatedAt": {
        "type": "integer",
        "description": "Timestamp of the last update to the source"
      },
      "filename": {
        "type": "string",
        "pattern": "^[a-zA-Z0-9_-]+\\.json$",
        "description": "Filename of the content JSON file relative to the data directory"
      },
      "randomize": {
        "type": "boolean",
        "description": "If true, items are shown in random order. If false, items are shown sequentially.",
        "default": false
      }
    },
    "required": ["name", "description", "updatedAt", "filename"],
    "additionalProperties": false
  }
}
